# Sprint Burndown

Build a remaining-vs-ideal sprint burndown for your team's delivery tickets, configured via the `SPRINT_BOARD` sentence in `.env`.

Read and execute `skills/sprint-burndown/SKILL.md` from the workspace root.

Do not stop until the on-track call is in your first reply and the burndown HTML report is written under `output/`.
